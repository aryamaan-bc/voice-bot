[project]
name = "bc-faq-agent"
version = "0.1.0"
description = "Basic Capital voice FAQ agent"
requires-python = ">=3.11"
dependencies = [
    "cartesia-line",
    "twilio>=9.0.0",
    "httpx>=0.27.0",
]

[tool.setuptools]
py-modules = ["main", "escalation", "slack_ticket", "linear_ticket"]
